import React from 'react';
import PropTypes from 'prop-types';
import TopMenu from '../../components/TopMenu';

Cart.propTypes = {

};

function Cart(props) {
  return (
    <div>
      <TopMenu />
      Cart
    </div>
  );
}

export default Cart;